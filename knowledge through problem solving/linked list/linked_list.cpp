/*.....................linked list....................................*/
/* how to insert data at the head
#include<bits/stdc++.h>
using namespace std;
// defining a node class
class node{
public:
        int data;
        node* next;
        // constructor for node
        node(int d){
            data=d;
            next=NULL;
        }
};
/*
class linkedlist{
    node*head;
    node*tail;
public:
    linkedlist(){
        head=NULL;
        tail=NULL;
    }
    void insert(){

    }
}
// passing a pointer variable by reference
void insertathead(node*&head,int data){
    node*n=new node(data);
    n->next=head;
    head =n;
}
/*
void print(node*head){
        node*temp=head;
        while(temp!=NULL){
        cout<<temp->data<<"-->";
        temp=temp->next;
        }

} or we can do is: 
void print(node*head){
        //node*temp=head;
        while(head!=NULL){
        cout<<head->data<<"-->";
        head=head->next;
        }

}
int main()
{
    node*head=NULL;
    insertathead(head,5);
    insertathead(head,4);
    insertathead(head,3);
    print(head);
    print(head);// eikhane 2bar print hobe but jodi pass by value kori taile 2nd time print hobe na karon head null
}
*/


// inserting at the middle or inserting at the end of the linked list

#include<bits/stdc++.h>
using namespace std;
class node                   
int main()
{

}


