 #include<bits/stdc++.h>
 using namespace std; 
 int main()
 {
    /* priority_queue<pair<int,int>>pq;   */

    /* sort hobe codeforce er rating onujai sorting kora
    5
    1 2
    2 3
    2 4
    4 4
    3 4
     */

    /* int n; cin>>n;
    for(int i=0; i<n; i++)
    {
        int a,b; cin>>a>>b;
        pq.push({a,(-1)*b});
    }
    while(!pq.empty())
    {
        cout<<pq.top().first<<" "<<pq.top().second*(-1)<<endl;
        pq.pop();
    } */
 }